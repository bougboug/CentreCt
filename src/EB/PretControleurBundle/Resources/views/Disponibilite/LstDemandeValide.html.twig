{% extends 'EBPretControleurBundle::layout.html.twig' %}

{% block EBPretcontroleur_body %}
                <div class="panel panel-blue margin-bottom-40">
                    <div class="panel-heading">
                        <h3 class="panel-title"><i class="fa fa-tasks"></i>Liste de mes demandes d'emprunt validés.</h3>
                    </div>
                    <div class="panel-body">
                        <p>Légende : </p>
                        <div class="table-responsive">                                    
                        <table class="table">
                          <tr>
                            <td><button class="btn btn-warning btn-xs">
                                <i class="fa fa-pencil"></i> 
                                  <font color="white">Annuler</font>
                                  </button>
                            </td>
                            <td>
                                Annuler une demande d'emprunt validé.
                            </td>
                          </tr>
                        </table>
                        </div>
                    </div>
                    <div class="table-responsive">                                         
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Mon centre</th>
                                <th>Controleur emprunté</th>
                                <th>Date</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                        {% for demande in listeDemandeRecu %}
                            <tr>
                            <td>{{ demande.centre.nom }}</td>
                            <td>{{ demande.controleur.nom }} {{ demande.controleur.prenom }} ({{ demande.controleur.centre.nom }})</td>
                            <td>{{ demande.date|date('d/m/Y') }}</td>
                            <td>
                              <a href="{{ path('eb_pret_controleur_AnnulerDemandeDisponibilite', {'idDisponibilite': demande.id}) }}">
                                <button class="btn btn-warning btn-xs"><i class="fa fa-pencil"></i> Annuler</button>
                              </a>
                            </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                    </div>
                </div>
{% endblock %}